import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        borderWidth: 1,
        margin: 10,
        height: 100,
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 5,
        borderColor: '#eceff1',
        flex: 1

    },

    title: {
        color: '#ffa041',
        fontSize: 20,
        fontWeight: 'bold',
    }
})