import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        borderWidth: 1,
        margin: 10,
        height: 100,
        width: '50%',
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 5,
        borderColor: '#eceff1'

    }

})